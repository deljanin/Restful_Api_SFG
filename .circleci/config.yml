version: 2.1
orbs:
  browser-tools: circleci/browser-tools@1.2.3
jobs:
  build:
    docker:
      - image: cimg/openjdk:19.0.2

    working_directory: ~/repo
    environment:
      MAVEN_OPTS: -Xms3200m

    steps:
      - checkout

      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "pom.xml" }}
            - v1-dependencies

#      - run: apt-get install mvn

      - run: mvn dependency:go-offline

      - save_cache:
          paths:
            - ~/.m2
          key: v1-dependencies-{{ checksum "pom.xml" }}
#      - run: mvn integration-test cobertura:cobertura

      - browser-tools/install-browser-tools

      - run: mvn integration-test jacoco:report



      - store_test_results:
          path: target/surefire-reports

      - store_artifacts:
          path: target

#      - run:
#          name: Send to CodeCov
#          command: bash <(curl -Os https://uploader.codecov.io/latest/linux/codecov && chmod +x codecov && ./codecov -t 7b1f26a2-f466-4832-9038-234271c5ab95)